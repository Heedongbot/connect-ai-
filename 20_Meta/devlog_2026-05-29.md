# NutriStack Lab — 작업 일지 2026-05-29

작업 시간: 2026-05-28 밤 ~ 2026-05-29 (진행 중)

---

## 1. 포스트 수동 수정 (3개)

### Copper (postId: 653447465802015470)
| 항목 | 수정 내용 |
|------|-----------|
| 이미지 | base64 인라인(755KB) → Imgur URL 복원 (T85vuL2.png, 391XfdA.jpeg) |
| FAQ | 완전 누락 → 6개 항목 수동 복원 |
| PMID | 3개 유지 확인 |
| H1 | "Copper Complete Guide: My Year..." → "Copper: My Year..." |
| og:title | JSON-LD headline과 일치화 |
| og:description | 한국어 혼입 → 영어 통일 |
| Related posts | 5개 제목 인간화 (AI 흔적 제거) |

### Iron (postId: 1947968909150831484)
| 항목 | 수정 내용 |
|------|-----------|
| 오탈자 | "Stockholm-based Stockholm-based" 중복 제거 |
| 오탈자 | "the the form" → "the form" |
| H1 | "Iron Complete: How I Actually..." → "Iron: How I Actually..." |
| JSON-LD | headline, og:title, Blogger 제목 전부 일치화 |
| Related posts | 5개 제목 인간화 (Copper와 동일 매핑) |

### SAMe (postId: 6798477625337286323)
| 항목 | 수정 내용 |
|------|-----------|
| TOC | `&#8594;` 리터럴 텍스트 노출 → `→` 직접 삽입 |
| DOCTYPE | `<!DOCTYPE html>` 전체 구조 → body 내용만 추출 |

**수정 스크립트:**
- `fix_copper_final.py` — Imgur URL 복원, FAQ 6개 복원
- `fix_iron_copper_final.py` — 종합 수정 (Iron + Copper)
- `fix_3items.py` — H1 정리, Related 인간화, og:description 영어화

---

## 2. 오케스트레이터 코드 수정 (v7.0)

파일: `00_NutriStack_Grand_Orchestrator_v5.py`

### 2-1. 패턴 다양화 — KT 박스 / TOC / Hook

```python
# Key Takeaways 박스 제목 5가지 랜덤 선택
KT_TITLES = ["Key Takeaways", "Before You Read On", "The Short Version", "What I Found", "Quick Summary"]

# TOC 스타일 4가지 (접두사 + 색상)
TOC_STYLES = [
    {"prefix": "→ ", "bg": "#f9f9f9", "border": "#ddd",    "link_color": "#2980b9"},
    {"prefix": "",   "bg": "#f5f5f5", "border": "#ccc",    "link_color": "#0066cc"},
    {"prefix": "· ", "bg": "#fafafa", "border": "#e0e0e0", "link_color": "#333"},
    {"prefix": "",   "bg": "#fff8f0", "border": "#d4a574", "link_color": "#8b5e3c"},
]

# KT 박스 색상 5가지
KT_THEMES = [
    ("#f0f7ff", "#2a6496"), ("#f0fff4", "#27ae60"),
    ("#fff8f0", "#e67e22"), ("#f9f0ff", "#8e44ad"), ("#f0f4ff", "#2c3e50")
]

# Hook 4가지 스타일 (assemble_post 내 _hook_style = random.randint(0,3))
# 0: <hr><p>...</p><hr>
# 1: colored left-border div italic
# 2: plain <p><em>...</em></p>
# 3: top/bottom border div
```

### 2-2. 과학 밀도 랜덤화 (science_density)

아키타입별 필드 추가:
```python
"science-heavy":       science_density = "high"    # 섹션당 PMID 2~3개
"minimalist":          science_density = "none"    # PMID 없음
"quick-answer":        science_density = "low"     # 0~1개
"journal-tone":        science_density = "medium"  # 1~2개
"nordic-anecdotal":    science_density = "low"
"comparison":          science_density = "medium"
"deep-protocol":       science_density = "high"
"comprehensive-guide": science_density = "high"
```

assemble_post에서 섹션별 PMID 삽입 여부를 density에 따라 결정.
PMID 표시 스타일도 3가지 랜덤 (blockquote / left-border p / inline italic).

### 2-3. URL 인간화

```python
URL_SEED_TEMPLATES = [
    "why-{nutrient}-finally-worked-for-me",
    "testing-{nutrient}-for-six-weeks",
    "what-{nutrient}-actually-did-for-me",
    "my-experience-with-{nutrient}",
    "tried-{nutrient}-for-30-days",
    ...
]

def generate_url_seed(nutrient_label: str) -> str:
    slug = re.sub(r'[^\w\s]', '', nutrient_label).strip().lower().replace(' ', '-')
    return random.choice(URL_SEED_TEMPLATES).format(nutrient=slug)
```

신규 포스트 발행 흐름:
1. url_seed 제목으로 insert → 짧은 인간적 URL 획득
2. 즉시 update로 실제 제목/내용 덮어쓰기
→ Blogger URL이 `why-magnesium-finally-worked-for-me` 형태로 생성됨

### 2-4. Python `.title()` 버그 수정

```python
_CASE_FIXES = {"Same": "SAMe", "Coq10": "CoQ10", "Nmn": "NMN",
               "Pqq": "PQQ", "Hmb": "HMB", "Nad": "NAD", "Epa": "EPA", "Dha": "DHA"}
```
→ SAMe, CoQ10 등 대소문자 혼합 영양소명 파괴 방지

### 2-5. DOCTYPE 버그 방지

```python
def _strip_html_document_wrapper(content: str) -> str:
    """AI가 전체 HTML 문서를 생성할 때 body 내용만 추출"""
```
→ publish_to_blogger() 호출 전 항상 실행

### 2-6. Phase Lock 제거

기존: `comparison / synergy / food-combo / antagonism` 타입은 comprehensive_guide 131개 완료 전까지 차단
→ **제거 완료**. 이제 모든 타입 candidates 평등하게 다음 작업 선정.

---

## 3. 스케줄러 코드 수정 (v5.6)

파일: `daily_scheduler_v5.py`

### 3-1. 자동 plan_today() 호출

```python
# register_today_schedule() 내
today_pending = [e for e in bank if e.get("date") == today and e.get("status") == "pending"]
if not today_pending:
    logging.info(f"  📅 오늘({today}) 스케줄 없음 → plan_today() 자동 실행")
    new_plan = plan_today()
    bank = load_topic_bank()
```
→ 기존: plan_today()를 수동 호출해야 했음 (버그). 이제 매일 자정 자동 실행.

### 3-2. 일일 발행 수 랜덤화

```python
post_count = random.choices([2, 3, 4], weights=[35, 45, 20], k=1)[0]
```
→ 2개(35%) / 3개(45%) / 4개(20%) 랜덤 — 인간적 발행 패턴

---

## 4. start_all.py 수정

파일: `start_all.py`

- 입력 대기 추가 (`input("아무 키나 누르면 닫힙니다...")`)
  → 더블클릭 시 결과 확인 가능
- `시작.bat` 배치 파일 추가 (chcp 65001 + 자동 디렉토리 이동)

---

## 5. 발견된 주요 버그 요약

| 버그 | 원인 | 수정 |
|------|------|------|
| TOC `&#8594;` 리터럴 노출 | CSS `content: "&#8594;"` → Blogger 렌더링 버그 | `→` 직접 삽입 + CSS 제거 |
| DOCTYPE 전체 저장 | LLM이 완전한 HTML 문서 생성 | `_strip_html_document_wrapper()` |
| Blogger double-encoding | GET `&#8594;` → PUT 그대로 → `&amp;#8594;` | `html.unescape()` before PUT |
| FAQ 잘림 | LLM 출력 truncation 또는 Blogger 길이 제한 | 수동 복원 (근본 원인 미해결) |
| base64 이미지 755KB | fallback PNG → Imgur → base64 | placeholder로 변경, 수동 교체 |
| Phase Lock | comparison/food-combo 수개월 차단 | 제거 |
| plan_today 미자동 실행 | register에서 plan_today 미호출 | 자동 호출 추가 |
| `.title()` 대소문자 파괴 | `SAMe.title() == "Same"` | `_CASE_FIXES` 딕셔너리 |
| og:title/desc 누락 | inject_seo_patch 미완성 | og:title + og:description 추가 |
| `list index out of range` 크래시 | `topic.encode('ascii','ignore')` 가 한국어 topic 전체 삭제 → `topic.split()[0]` 에서 IndexError | ASCII 강제변환 제거, BOM/제어문자만 제거로 변경 |
| 90%+ 품질도 Critic 3회 반려 | `_blocking_issues` 있으면 87.5% 스킵 조건 무력화 | 90%+ → Critic 완전 스킵, 1회 반려 즉시 Claude 재작성 |

---

## 6. v7.1 추가 수정 (2026-05-29 야간)

### 크래시 수정 — ASCII 스트리핑 제거

```python
# 이전 (버그):
topic = topic.encode('ascii', 'ignore').decode('ascii')  # 한국어 topic → ""

# 수정 후:
topic = topic.lstrip('﻿​‌‍')  # BOM + zero-width chars only
topic = re.sub(r'[\x00-\x08\x0b\x0c\x0e-\x1f\x7f]', '', topic)  # 제어문자
```

### 안전 가드 — URL seed에서 빈 topic 처리

```python
_topic_words = [w for w in topic.split() if re.sub(r'[^\w]','',w)]
_nutrient = re.sub(r'[^\w\s]', '', _topic_words[0] if _topic_words else "supplement").strip().lower() or "supplement"
```

### Critic 승인 로직 변경 (v7.1)

```
90%+ → Critic 스킵, 즉시 발행 (post-publish 검증이 처리)
80%+ + 치명적이슈 없음 → Critic 스킵
그 이하 → Critic 1회만 실행 → 반려 즉시 Claude 재작성 (3회 루프 제거)
```

**Why:** 90% 품질도 blocking_issues 하나만 있으면 3회 반려 루프 → 수십 분 낭비.
발행 후 post-publish verifier(Claude)가 어차피 수정하므로 중복 검증 불필요.

---

## 7. 오늘 기준 현황

- 완료 포스트: 10개 (오늘 3개 예약됐으나 크래시로 0개 발행)
- topic_bank 진행: 13/131 comprehensive_guide 완료
- 오늘 예약: 08:23 / 12:11 / 16:29 모두 크래시 (list index out of range)
- 실행 중 프로세스: orchestrator (포트 19999), scheduler (포트 19998)

---

## 8. post_quality_check + post_publish_verifier 개선

**Why:** verifier가 추상적 지표(단어수, Hook길이)만 체크하고 실제 반복 버그(Complete Guide, 이중인코딩, TOC 깨짐)를 안 잡음.

### post_quality_check.py 추가 체크 항목

| 항목 | 추가 내용 |
|------|-----------|
| A1 (제목 오염) | `BAD_TITLE_WORDS`에 "Complete Guide", "Complete" 추가 |
| B1 (og:description) | 한국어 혼입 시 score 0 처리 |
| D4 (HTML 구조) | DOCTYPE 전체문서 버그, &amp;# 이중인코딩, &#8594; 잔존 체크 추가 |

### post_publish_verifier.py 자동 수정 추가

발행 후 **카테고리 채점과 무관하게 항상 실행**되는 버그 수정:
- `_fix_d4_html_bugs()`: &amp;# 이중인코딩 해제, &#8594;→ 교체, DOCTYPE 추출
- `_fix_a1_complete_guide()`: H1에서 "Complete Guide" / "Complete" 자동 제거
- `_fix_b1_og_desc_korean()`: 한국어 혼입 감지 → 레슨 기록 (자동수정 불가)

---

*다음 세션에서 확인할 것:*
- [ ] v7.1 수정 후 발행 정상 작동 확인
- [ ] post-publish verifier 새 체크 항목이 실제로 버그 잡는지 확인
- [ ] claude_discoveries.json 쌓이는지 확인 (발행 후)
- [ ] FAQ 잘림 근본 원인 조사 (Blogger API 길이 제한 vs LLM truncation)
